from enum import IntEnum
import numpy as np


def compute_mask(min, max, weights,images):

    
    return weights


def gaussian(arr, mean, deviation):
    return np.exp(-(arr-mean)*(arr-mean)/2/deviation/deviation)


def get_fusion_weights(images, median):

    return image_weights


def raw_exposure_fusion(images, median, exposure_times, mask_percent=0.03):

    return  


def bilateral_filtering(image, range_sigma, spatial_sigma):
 
    return  

def fastbilateral2d(rgb_img: np.array, height, contrast, range_sigma=0.4):


    return 